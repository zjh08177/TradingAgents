{
  "project_name": "Trading Graph Server",
  "description": "AI-powered trading analysis system using LangGraph",
  "commands": {
    "trace:analyze": {
      "description": "Analyze LangSmith trace for performance optimization",
      "script": ".claude/commands/trace/analyze.sh"
    },
    "eric:cleanup": {
      "description": "Clean up stale files and optimize disk usage",
      "script": ".claude/commands/eric/cleanup.sh"
    }
  },
  "hooks": {
    "post-command": [
      {
        "pattern": "/trace:analyze",
        "action": "echo 'Trace analysis complete. Check the generated report.'"
      },
      {
        "pattern": "/eric:cleanup",
        "action": "echo 'Cleanup complete. Disk space optimized.'"
      }
    ]
  },
  "environment": {
    "LANGCHAIN_API_KEY": "${LANGCHAIN_API_KEY}",
    "LANGCHAIN_TRACING_V2": "true",
    "LANGCHAIN_PROJECT": "trading-agent-graph"
  }
}